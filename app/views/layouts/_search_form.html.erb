<%= form_with(url: search_path, local: true) do |f| %>
  <%= f.text_field :content %>
  <%= f.select :model, [['user', 'user'], ['book', 'book']], selected: 'user' %>
  <%= f.select :method, [['完全一致', 1], ['前方一致', 2], ['後方一致', 
  3], ['部分一致', 4]], selected: 1 %>
  <%= f.submit "検索"%>
<% end %>
